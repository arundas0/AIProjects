# AI Daily Assistant (Local-First)

Phase 0: CLI that calls a local LLM via Ollama.

## Run
1) Install Ollama + pull a model (e.g. llama3.2:3b)
2) Create venv:
   python3 -m venv .venv
   source .venv/bin/activate
   pip install -r requirements.txt
3) Run:
   python assistant_cli.py
